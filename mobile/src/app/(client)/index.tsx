import { AuthenticatedPlaceholder } from "@/ui/authenticated-placeholder";

export default function AuthenticatedRootScreen() {
  return <AuthenticatedPlaceholder title="Authenticated shell preview" />;
}
